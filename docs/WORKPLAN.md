# WORKPLAN

## Current Mission: Phase 2 - Preparation & Analytics Dashboard

### Tasks
- [ ] 0. **Routing Migration**: Remove Hash Routing (`/#/uuid`) and revert back to Standard Routing (`/uuid`), setting up a 404 redirect script for GitHub Pages compatibility.
- [ ] 1. Move DEV_MODE_QUERY_PARAM in App.tsx to utils
- [ ] 2. Move helper functions in App.tsx to utils
- [ ] 3. Move helper func in StatsTab.tsx and TrackerTabs to utils
- [ ] 4. Verify all files for variables and everything that can move to constants and utils and move them
- [ ] 5. Before git push, make sure we are not in main branch. Create a new feature branch and push to it.
- [ ] 6. **Dashboard**: Build a simple unified dashboard view for richer analytics. // MOVE TO PHASE 3
